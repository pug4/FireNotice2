# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'FireNotice' do
  # Comment the next line if you don't want to use dynamic frameworks
  
  use_frameworks!
  # Pods for FireNotice
  pod 'Firebase/Firestore'
  pod 'Firebase/Messaging'
  pod 'Firebase/Core'
  pod 'Firebase'

end
